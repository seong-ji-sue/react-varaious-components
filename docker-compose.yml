version: '3.9'

services:
  client:
    image: client:latest
    container_name: client
    ports:
      - "3000:3000"
    env_file:
      - .env.production
#  server:
#    image: server:latest
#    container_name: server
#    ports:
#      - "3001:3001"
#    env_file:
#      - .env.production
  iam:
    image: iam:latest
    container_name: iam
    ports:
      - "3002:3002"
    env_file:
      - .env.production
  vault:
    image: vault:latest
    container_name: vault
    ports:
      - "3003:3003"
    env_file:
      - .env.production
  fabric:
    image: fabric:latest
    container_name: fabric
    ports:
      - "3004:3004"
    env_file:
      - .env.production
  kms:
    image: kms:latest
    container_name: kms
    ports:
      - "3005:3005"
    env_file:
      - .env.production